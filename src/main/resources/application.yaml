spring:
  application:
    name: payments
  datasource:
    url: jdbc:postgresql://localhost:5432/payments
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true